---
deployment:
  tasks:
    - export PROJECTPATH=/home/erento/public_html/vertical-shared-modules/test-folder/
    - /bin/rm -Rf $PROJECTPATH
    - /bin/mkdir $PROJECTPATH
    - /bin/cp -R components $PROJECTPATH
